import { View, ScrollView, Text } from 'react-native'
import React from 'react'

function TodoList() {
    return (
        <ScrollView>
            <Text>TodoList</Text>
        </ScrollView>
    )
}

export default TodoList